# COMMON.md

本文件包含跨项目通用的开发准则，适用于所有使用 Claude Code 开发的项目。

## 交流准则

### 语言使用

**交流时用中文，除非是非常专业的词汇**

- 使用中文进行描述、注释、文档和讨论
- 通用技术术语可保留英文（如：API、SDK、JSON、Git）
- 避免中英文混杂的表达方式

## 设计原则

### 最小入侵原则

**核心理念**: 优先通过配置实现功能，最小化代码修改范围

- **配置优先**: 通过配置表实现，避免修改核心代码
- **单一修改点**: 集中在配置文件或特定函数
- **渐进增强**: 从最小可行实现开始
- **标识驱动**: 用标识符（如 `user_code`）区分行为，非复杂继承

### 代码继承原则

**决策准则**: 根据分支数量和复杂度选择实现方式

- 分支 ≤ 5 个用 switch/case
- 分支 ≥ 10 个考虑继承
- 分支逻辑 ≥ 50 行考虑抽象类
- 重复行为模式（如飞行/动画/碰撞）提取基类
- 简单场景避免虚函数开销

### 骨架优先原则（Skeleton First）

**核心纲领**: 先完成骨架验证可行性，再逐步增强

**三阶段策略**:
1. **骨架**: 端到端跑通，代码紧凑（100-200行），逻辑清晰
2. **血肉**: 限流保护、错误处理、日志监控
3. **皮肤**: 性能优化、安全加固、架构调整

**最小可验证子集标准**:
- 能跑通（端到端验证）
- 易理解（无过度抽象）
- 易扩展（方便加功能）
- 易调试（快速定位）

**避免的陷阱**:
- ❌ 一次性实现所有功能
- ❌ 多层抽象、复杂目录
- ❌ 为"未来可能"需求提前实现
- ❌ 假大空的"企业级"代码

## 代码规范

### 缩进规范（强制）

**所有语言必须使用 2 空格，禁止 Tab**

## Git 提交规范

**简洁干练原则**:
- 标题：`类型(范围): 描述`（≤50 字符）
  - 类型：feat/fix/refactor/docs/test/chore 等
  - 范围：模块名或功能区域
  - 描述：祈使句，不说废话
- 必须包含：`Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>`

**示例**:
- `feat(fishtank): 添加沙子气泡系统`
- `fix(auth): 修正登录超时处理`
- `refactor(parser): 提取词法分析器基类`

## Claude 开发思考约束

### 远虑近做原则

**第一层：远虑（设计阶段）**
- 完整分析所有扩展场景（性能、内存、维护影响、系统集成）
- 在 docs/ 记录设计决策
- 预留扩展接口但不实现

**第二层：近做（实现阶段）**
- 选择最简单解决方案
- 最小化代码修改范围
- 避免过度工程化
- 保持向后兼容

**执行流程**: 远虑分析 → 文档记录 → 近做实现

**约束检查清单**:
- [ ] 完整分析扩展场景？
- [ ] 在 docs/ 记录设计决策？
- [ ] 选择最简单方案？
- [ ] 最小化修改范围？
- [ ] 避免不必要复杂性？
- [ ] 保持向后兼容？

---
*创建日期: 2026年2月13日*
